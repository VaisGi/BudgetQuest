<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Note: Compose handles all theming. This is only used as a minimal window config. -->
    <style name="Theme.BudgetQuest" parent="@android:style/Theme.DeviceDefault.NoActionBar" />
</resources>
